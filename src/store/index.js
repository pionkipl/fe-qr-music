import { createStore } from 'vuex'
import songs from './states/songs'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    songs
  }
})
